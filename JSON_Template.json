{
	"name": String,
	"score": int,
	"minScore": int,
	"briefing": String,
	"defaultActionError": String,
	"states":[
		{
			"id": int,
			"name": String,
			"startState": int,
			"endState": int,
			"signals": [
				{
					"id": int, // Id of the instance inside State
					"signalTemplateId": int, // Id of the signal template, from .csv
					"type": int,
					"name": String,
					"value": String
				}
			],
			"tips": [
				{
					"id": int,
					"message": String,
					"actorType": String,
					"actorName": String,
					"activationTime": float, // percentage [0,1] (related to state duration) / OPTION: -1 means that it has no specific time to show
					"duration": float,
					"actionsDone": [int], // action IDs
					"actionsTodo": [int] // action IDs
				}
			],
			"transition": {
				"id": int, // TODO: Remove?
				"duration": float,
				"stateId": int
			}
		}
	],
	"actions": [
		{
			"id": int,
			"name": String,
			"type": {
				"id": int, // Type ID accordingly to action_types.csv
				"name": String // This is the same as action.name
			},
			"category": {
				"id": int, // Category type ID accordingly to action_categories.csv
				"name": String
			},
			"subcategory": {
				"id": int, // Subcategory type ID accordingly to action_subcategories.csv
				"name": String // This is the same as action.name
			}, // TODO only in JSON: [{category},{subcategory}]
			"complementaryActions": [int], // Complementary Actions IDs
			"behavior": String,
			"effectTime": float,
			"usageLimit": int,
			"conditions": [int], // state IDs
			"results": [ // array signals
				{
					"id": int,
					"signalTemplateId": int, // Id of the signal template, from .csv
					"type": int,
					"name": String,
					"value": String
				}
			],
			"transition": {
				"id": int, // TODO: Remove?
				"stateId": int
			},
			"score": {
				"startStates": [int], // state IDs
				"endStates": [int], // state IDs
				"lostValue": float, // Immediately lost value
				"lossOvertime": float // Value that will be lost overtime (until completion...)
			},
			"errorMsg": { // TODO
				"actorType": String,
				"actorName": String,
				"message": String
			}
		}
	]
}